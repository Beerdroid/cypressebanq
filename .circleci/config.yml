version: 2.1

orbs:
  cypress: cypress-io/cypress@1.29.0
  
workflows:
  build:
    jobs:
      - cypress/run:
          post-install:
          - run: npm run clean
          - run: npm run test || true
